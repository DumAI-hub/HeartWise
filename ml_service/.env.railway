# ML Service Environment Variables for Railway
# Copy these to Railway dashboard when deploying

# Port (Railway sets this automatically, but you can override)
PORT=8000

# No other environment variables needed
# Models are included in the repository and loaded automatically
